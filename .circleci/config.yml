version: 2

jobs:
  build:
    docker:
      - image: madduci/docker-ubuntu-cpp:gcc-6 
    steps:
      - checkout
      - run:
          name: cmake
          command: cmake .
      - run:
          name: make
          command: make
